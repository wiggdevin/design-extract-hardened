name: Dependency auto-merge disabled
on:
  workflow_dispatch:
permissions: {}
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Dependency changes require explicit review and verification'
