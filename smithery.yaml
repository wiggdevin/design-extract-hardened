startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      outputDir:
        type: string
        default: ./design-extract-output
        description: "Directory containing files produced by `designlang <url>` (must include *-mcp.json)."
    required: []
  commandFunction: |-
    (config) => ({
      command: 'node',
      args: ['/app/bin/design-extract.js', 'mcp', '--output-dir', config.outputDir || './design-extract-output'],
    })

build:
  dockerfile: smithery.dockerfile
